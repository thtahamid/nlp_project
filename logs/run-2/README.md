# Run 02 — Logs

**Date:** 2026-04-18
**Base model:** `Qwen/Qwen2.5-VL-7B-Instruct` (4-bit NF4, bf16 compute)
**Adapter:** `models/lora_adapter/run-2/` (LoRA r=32, α=64, dropout=0.05)
**Training data:** 1120 train / 280 eval (full split)
**Hardware:** NVIDIA A10G (23 GB VRAM)

This directory holds every artefact produced by Run 02 of the Arabic HTR fine-tune: training-loss tables, evaluation metrics, and the analysis write-ups that go with them. Reports for Run 01 live in `logs/run-1/`.

## What's in this folder

| File | Purpose |
|------|---------|
| `01_training_run.md` | Full training report — hyperparameters, loss curve, wall-clock, comparison to Run 01, and the overfitting observation that makes `checkpoint-1120` the recommended pick. |
| `02_evaluation_run.md` *(pending)* | Evaluation report for the chosen checkpoint on the 280-sample held-out set — CER / WER / exact-match / valid-JSON / latency, plus qualitative analysis of worst predictions. |
| `eval_results.json` *(pending)* | Machine-readable summary written by `notebook/06_evaluation.ipynb` for the merged final adapter (`checkpoint-1680`). |
| `eval_results_checkpoint-1120.json` *(pending)* | Same schema, produced when `CHECKPOINT='checkpoint-1120'` is set in Notebook 6 §4. Used to verify the epoch-2 checkpoint beats epoch-3 on CER/WER. |

*(pending)* files are generated by running `notebook/06_evaluation.ipynb` — switch `RUN_NAME` / `CHECKPOINT` in the config cell to target a different adapter.

## Training at a glance

| Metric | Value |
|--------|-------|
| Total steps | 1680 (3 epochs × 560 steps) |
| Effective batch size | 2 (1 × 2 grad accum) |
| Final train loss | 1.4550 |
| Final eval loss | 1.6387 (step 1680) |
| **Best eval loss** | **1.6248 (step 1120, end of epoch 2)** |
| Wall-clock | ~44 min (≈38 steps/min) |
| Eval pass / epoch | ~37 s |

Eval loss improves through epoch 2 then ticks back up in epoch 3 while train loss keeps falling — a mild overfitting signal. See `01_training_run.md` for the full loss table and analysis.

## How to reproduce

1. **Training** — run `notebook/05_finetuning.ipynb` end-to-end. The run name in the config cell is `run-2`; outputs land in `models/lora_adapter/run-2/` and `logs/run-2/`.
2. **Evaluation (final adapter)** — run `notebook/06_evaluation.ipynb` with `RUN_NAME='run-2'` and `CHECKPOINT=None`. Writes `eval_results.json`.
3. **Evaluation (best checkpoint)** — set `CHECKPOINT='checkpoint-1120'` and rerun §5 onward. Writes `eval_results_checkpoint-1120.json`.

## Related artefacts (outside this folder)

- `models/lora_adapter/run-2/` — merged final adapter + `checkpoint-1120` + `checkpoint-1680`
- `data/train/train.jsonl`, `data/eval/eval.jsonl` — 1120 / 280 samples with `gpt-5.4-mini` reference transcriptions
- `docs/explainers/` — interactive pipeline diagrams, concepts guide, glossary, and the project report
